## Conclusions {#sec-conclusions}

{% set L = ctx.law_stats %}{% set T = ctx.theory_stats %}
Of the {{ L.n }} candidate laws surfaced by the search, {{ L.held }} reproduced and survived a negative-control audit on independent re-analysis{% if ctx.held_law_ids %} ({% for i in ctx.held_law_ids %}{{ ref(i) }}{% if not loop.last %}, {% endif %}{% endfor %}){% endif %}; {{ L.partial }} held only partially and {{ L.untestable }} could not be tested on the data in hand. Of {{ ctx.n_theories }} theories generated, {{ T.tested }} were testable now and {{ T.held }} {{ 'was' if T.held == 1 else 'were' }} supported on verification{% if ctx.held_theory_ids %} ({% for i in ctx.held_theory_ids %}{{ ref(i) }}{% if not loop.last %}, {% endif %}{% endfor %}){% endif %}.{% if ctx.definitional_ids %} {% for i in ctx.definitional_ids %}{{ ref(i) }}{% if not loop.last %}, {% endif %}{% endfor %} {{ 'is' if ctx.definitional_ids|length == 1 else 'are' }} confirmed but definitional — true by construction rather than an empirical discovery.{% endif %} The remaining theories are untested pending the data gaps noted in Trustworthiness.
