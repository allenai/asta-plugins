# Quarto build artifacts (the workspace skill appends any of these lines
# missing from the project's .gitignore).
_site/
.quarto/
quarto-render.*

# Fetched from the workspace skill before each render (tracks asta-plugins main
# by default; see the workspace-assets target in Makefile).
_extensions/evidence/
