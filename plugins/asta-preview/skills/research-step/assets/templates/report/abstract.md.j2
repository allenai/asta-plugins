## Mission {#sec-abstract .unnumbered}

The mission below is the *input* to this workflow; this report is its *output*. The [workflow](#sec-workflow) that follows was executed against this mission, and every section is generated deterministically from its results.

```{=latex}
\begin{contextbox}
```
{% if ctx.mission %}{{ ctx.mission|clean }}{% else %}This report reproduces the findings of an Asta AutoDiscovery run on independent data and then generates literature-grounded theories from the reproduced relationships. Every step was executed by Asta's automated agents, and each verdict is deterministic and traceable to its run artifacts.{% endif %}
```{=latex}
\end{contextbox}
```
