## Summary {#sec-abstract}

{% set L = ctx.law_stats %}{% set T = ctx.theory_stats %}
{% if ctx.primary_dataset %}{% set dd = ctx.primary_dataset.definition|clean %}{% set dp = short(dd.split(': ', 1)[1] if ': ' in dd[:40] else dd, 150) %}This study reproduces and theorizes the findings of an Asta AutoDiscovery run over {{ dp|mathify }}{{ '' if dp.endswith('…') else '.' }} {% else %}This study reproduces and theorizes the findings of an Asta AutoDiscovery run. {% endif %}Every step was executed by Asta's automated agents ({{ ctx.agents_used|join(', ') }}), and each verdict is deterministic and traceable to its run artifacts.

The run surfaced **{{ L.n }} candidate hypotheses**, which we reproduced on independent re-analysis under prespecified tests, each guarded by a negative-control audit: **{{ L.held }} held**{% if ctx.held_law_ids %} ({% for i in ctx.held_law_ids %}{{ ref(i) }}{% if not loop.last %}, {% endif %}{% endfor %}){% endif %}, {{ L.partial }} {{ 'was' if L.partial == 1 else 'were' }} partial, and {{ L.untestable }} could not be tested on the data in hand. We then generated **{{ ctx.n_theories }} literature-grounded theories** under {{ ctx.objectives|length }} objectives ({{ ctx.objectives|map('replace','_focused','')|join(' and ') }}) and triaged each for testability; **{{ ctx.n_testable_theories }}** {{ 'was' if ctx.n_testable_theories == 1 else 'were' }} testable now, each committed to a prespecified test, and the rest await new data. The theories are proposed and triaged here, not yet verified.
