## Abstract {#sec-abstract}

{% set L = ctx.law_stats %}{% set T = ctx.theory_stats %}
{% if ctx.primary_dataset %}{% set dd = ctx.primary_dataset.definition|clean %}{% set dp = (dd.split(':')[1] if ':' in dd else dd)|trim %}This study reproduces and theorizes the findings of an Asta AutoDiscovery run over {{ dp|truncate(150, false, '…') }}. {% else %}This study reproduces and theorizes the findings of an Asta AutoDiscovery run. {% endif %}The run surfaced **{{ L.n }} candidate empirical laws**, which we reproduced on independent re-analysis under prespecified tests, each guarded by a negative-control audit: {{ L.held }} held, {{ L.partial }} {{ 'was' if L.partial == 1 else 'were' }} partial, and {{ L.untestable }} could not be tested on the data in hand. We then generated **{{ ctx.n_theories }} literature-grounded theories** under {{ ctx.objectives|length }} objectives ({{ ctx.objectives|map('replace','_focused','')|join(' and ') }}); {{ T.tested }} were testable on the available data and taken to verification, of which **{{ T.held }} held** and {{ T.partial }} {{ 'was' if T.partial == 1 else 'were' }} partial. Every step was executed by Asta's automated agents ({{ ctx.agents_used|join(', ') }}), and each verdict is deterministic and traceable to its run artifacts.
