[project]
name = "asta-paper-finder"
version = "0.1.0"
description = "CLI for Asta Paper Finder"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
dependencies = []

[tool.setuptools]
py-modules = ["asta_paper_finder"]

[tool.uv]
dev-dependencies = []
